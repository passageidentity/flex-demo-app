{
  "name": "root",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "build": "lerna run build --stream",
    "dev": "lerna run dev --stream",
    "setup": "lerna run setup --stream"
  },
  "dependencies": {},
  "devDependencies": {
    "lerna": "^8.0.2"
  }
}
